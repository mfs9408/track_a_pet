import { StyleSheet } from "react-native";

export const makeStyles = () =>
  StyleSheet.create({
    header: {
      marginBottom: 30,
    },
    upperParagraph: {
      marginBottom: 5,
    },
  });
