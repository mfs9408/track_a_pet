import { StyleSheet } from "react-native";

export const makeStyles = () =>
  StyleSheet.create({
    header: {
      marginBottom: 30,
    },
    formContainer: {
      marginBottom: 20,
    },

  });
