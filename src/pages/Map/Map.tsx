import React from "react";
import { SafeAreaView, ScrollView } from "react-native";

const Map = () => {
  return (
    <SafeAreaView>
      <ScrollView></ScrollView>
    </SafeAreaView>
  );
};

export default Map;
